APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:hfhTuNbm2U+cakRuvgYQig+6QjroYNMhX7iqWmvs+FY=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=https://bluehub.zotecsoft.work/
APP_PATH=/var/www/blueHubCloud
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=pbx-reporting-db.crqiko0q2fil.ap-southeast-2.rds.amazonaws.com
DB_PORT=3306
DB_DATABASE=pbx-reporting-db
DB_USERNAME=admin
DB_PASSWORD="0cXKZ~Dl_#bW6~u|zld5oXTQ$T89"

# DB_PASSWORD=NJV)4!hgP3IHaC$D!Y:3<0Ozc54g
# DB_CONNECTION=mysql
# DB_HOST=localhost
# DB_PORT=3306
# DB_DATABASE=bluehub_local
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# AWS credential source: if `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` are set,
# the application will use those static credentials. If left empty, the AWS SDK
# will fall back to its default provider chain (shared config, environment, or
# instance profile / IAM role). Behaviour is controlled entirely by these env
# values — there are no enforced environment restrictions.
AWS_DEFAULT_REGION=ap-southeast-2
AWS_USE_PATH_STYLE_ENDPOINT=false
 
VITE_APP_NAME="${APP_NAME}"

PBXWARE_AWS_REGION=ap-southeast-2
# Controls scheduled PBX ingest (true/false)
PBXWARE_INGEST_ENABLED=false
# PBXWARE_MOCK_MODE: Toggle PBX behaviour. This flag is the single source of truth
# for PBX client behaviour. Set to `true` to force the application to use the
# local/mock PBX client and test fixtures. Set to `false` to use the real PBX API
# and fetch credentials from Secrets Manager or environment. The application will
# NOT block or change behaviour based on APP_ENV — using mock mode in production
# is supported for testing, and using real mode locally is allowed. Operator
# control via .env is authoritative.
PBXWARE_MOCK_MODE=false

# PBXware credentials
#
# Production (preferred): AWS Secrets Manager
# - Secret name: pbxware/api-credentials
# - Expected JSON keys:
#   - api_key
#   - base_url
#   - server_id
#   - timeout (optional)
#
# Testing / recovery (fallback ONLY): Environment variables
# Used only if Secrets Manager is missing/unavailable or incomplete.
PBXWARE_API_KEY=Z4U9XacRyiddUPjsLavt9PCDKFgeD2x1
PBXWARE_BASE_URL=https://ip.pbxbluehub.com
PBXWARE_SERVER_ID=1
PBXWARE_TIMEOUT=60

# PBXware Secrets Manager options
PBXWARE_DISABLE_SECRETS_CACHE=false
AWS_SECRETS_DEBUG=false



